// import React from 'react';
// import styles from '..//NavBar.module.css';

// const Icon = ({ faclassName, colour, size, myclassName }) => {
//   return (
//     <div style={{ width: `${size}`, height: `${size}` }}>
//       <i
//         style={{ color: `${colour}`, width: '100%', height: '100%' }}
//         className={`${styles.icon} ${myclassName}`}></i>
//     </div>
//   );
// };

// export default Icon;
